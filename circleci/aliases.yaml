- name: launch_job
  desc: Choose a job from ci configuration and run it locally 
  alias: 'circleci local execute --job={{jobs}}'

- name: check_configuration
  desc: validate your circleci configuration
  alias: 'circleci config validate'

