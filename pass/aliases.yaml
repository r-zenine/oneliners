- name: password
  desc: Choose a password and put it in the clipboard
  alias: pass -c {{ service }}

- name: otp
  desc: Choose a totp token and put it in the clipboard
  alias: pass otp -c {{ service }}
